FILESYSTEM  (deny-by-default; every line is a grant, there are no deny rules)
  dev    /dev                                           (snug)
  ro     /etc (from /host/etc-ancestor)                 marks
                     ← 17 paths SUPPLIED (17 command tables, 0 credentials):
                       /etc/gitconfig, /etc/claude-code/managed-settings.json,
                       /etc/claude-code/managed-settings.d, +14 more.
  proc   /proc                                          (snug)
  tmpfs  /tmp                                           (snug)
  ro     /usr (from /host/usr-src)                      marks
  ro     /etc/gitconfig (from /host/gitconfig-src)      marks
                     ← COMMAND TABLE: git reads this as its system config, read
                       with no flag. Read-only SUPPLIES credential.helper, alias
                       = !cmd, core.pager, core.sshCommand.
  data   /etc/resolv.conf                               (snug)
  ro     /opt/keys (from /home/u/.ssh)                  marks
                     ← the host's ~/.ssh is exposed inside - CREDENTIAL: private
                       keys.
  ro     /usr/share (from /host/usr-share-src)          marks
  ro     /etc/claude-code/managed-settings.json (from /host/managed-settings-src) marks
                     ← COMMAND TABLE: claude reads this as managed (enterprise)
                       settings, read with no flag. Read-only SUPPLIES
                       policyHelper, hooks, mcpServers, apiKeyHelper, env.
  link   /home/u/.bashrc -> /wherever                   marks
                     ← COMMAND TABLE: bash reads this. Read-only SUPPLIES runs
                       on every shell.
  ro     /home/u/.netrc                                 marks
                     ← CREDENTIAL PATH: curl (and anything else honouring
                       .netrc) reads this; whatever is here is what it
                       authenticates with.
  ro     /home/u/.npmrc                                 marks
                     ← COMMAND TABLE: npm reads this. Read-only SUPPLIES
                       registry auth tokens, and script hooks.
  ro-/   everything else is a read-only skeleton (--remount-ro /)